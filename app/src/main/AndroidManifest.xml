<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.filmingapp">
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.FilmingApp">
        <activity android:name=".ui.Peliculas.PeliculasImagenDetalles"
            android:theme="@style/Theme.AppCompat.NoActionBar"
            android:parentActivityName=".ui.Peliculas.PeliculasDetalles">

        </activity>
        <activity android:name=".ui.Peliculas.PeliculasDetalles" />

        <activity android:name=".ui.Series.SeriesImagenDetalles"
            android:theme="@style/Theme.AppCompat.NoActionBar"
            android:parentActivityName=".ui.Series.SeriesDetalles">

        </activity>
        <activity android:name=".ui.Series.SeriesDetalles" />


        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/Theme.FilmingApp.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>